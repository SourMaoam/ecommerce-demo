Start the development servers for both frontend and backend.

Follow these steps:
1. Open a new terminal for the backend
2. Navigate to EcommerceApi folder
3. Run `dotnet run`
4. Open another terminal for the frontend  
5. Navigate to ecommerce-frontend folder
6. Run `npm start`
7. Verify both servers are running correctly
